export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    // '/dashboard',
    // '/coding-assistant',
    // '/quiz-generation',
    // '/real-time-text-tutoring',
    // '/study-guide',
    // '/summarization',
  ],
};
